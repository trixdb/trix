# Handoff — Session b940a1f2 (robert profile, blocked until 2026-04-15 14:00 Europe/Stockholm)

## Sprint progress (last updated 2026-04-14 20:15 Europe/Stockholm)

| Phase | Progress | Status |
|-------|----------|--------|
| **P3.5** `answer_format=extractive` on `/v1/chat` | `████████████████████` 100% | ✅ Shipped |
| **P4** MemoryPipeline facade + envelope/runner/stream | `████████████████████` 100% | ✅ Shipped |
| **P4b** Composite preset schema + composability primitives | `████████████████████` 100% | ✅ Shipped |
| **P5** Endpoint migrations (7/7 retrieval endpoints + suggest-strategy) | `████████████████████` 100% | ✅ Closed |
| **AgentPipeline** scaffold (start/approve/cancel ops + 53 tests) | `████████████████████` 100% | ✅ Scaffold |
| **P6** Stage extraction (translators → stage-local) | `████████████████████` 100% | ✅ Shipped |
| **Output schemas** declared on all 5 memory ops | `████████████████████` 100% | ✅ Shipped |
| **`?trace=stages`** admin-gated debug flag | `████████████████████` 100% | ✅ Shipped |
| **DB-backed preset table** + CRUD + 8-route threading + CLI | `████████████████████` 100% | ✅ End-to-end (incl. `trix pipeline`) |
| **Golden fixtures** (42 fixtures, 7 endpoints) | `████████████████████` 100% | ✅ Shipped |
| **P7** Write-side `consolidation` block (2 endpoints) | `████████████████████` 100% | ✅ Closed (consolidate + conflicts/detect) |
| **P8** Graph block + 6 endpoint migrations | `████████████████████` 100% | ✅ Closed (traverse/context/path/neighbors/expand/deep-recall) |
| **P10** Ingestion namespace — schema + endpoint + payload + worker fact_observer gate | `████████████████░░░░` 80% | ✅ Worker honors fact_observer/entity_extract; ⏳ session/mega/scoped processors |

Living status doc: `trix-research/docs/findings/2026-04-14-pipeline-rollout-status.md`.

## Goal
Make trix-api's memory/agent pipelines SOTA. Driver: prod scores lag benchmark-harness scores because improvements weren't incorporated into trix-api itself. Plan is to consolidate into a universal pipeline facade used by every surface area (memory, agents, chat, ingestion) with swappable presets.

## Context docs (authoritative)
- `trix-research/docs/findings/2026-04-13-memory-surface-audit.md`
- `trix-research/docs/findings/2026-04-13-pipeline-consolidation-design.md`
- `trix-research/docs/reviews/RADICAL_REVIEW_2026-04-12.md`
- `trix-research/docs/roadmap/SHIP_ROADMAP_2026-04-12.md`
- ADRs: `ADR-111` (configurable pipelines everywhere), new `ADR-112` (memory-pipeline universal entrypoint), new `ADR-113` (pluggable operation registry)

## 13 findings from the 5-agent audit (plan of record)

Pre-benchmark quick wins:
1. ~~Fix `fromInline()` Ajv validation gap (#4)~~ — done
2. ~~Flip 4 stale ADR statuses (#11)~~ — done (ADR-108/109/110/111 moved planned→completed)
3. ~~`modelMatchesAllow` removal (#10)~~ — **skipped, audit was wrong** (it IS used in route + chat-service)
4. ~~Break chat↔registry cycle via `support.twoPassOp` injection (#9)~~ — done via new `support.invoke()`
5. Two-pass recursion regression + sync-throw tests (#7, #8) — tests added; **regression run was in flight when limit hit**
6. ADR-103/112 cross-reference + `memory_pipeline_name` migration (#2) — migration agent finished (7 route + 3 resolver + 3 migration tests)

Architectural:
7. Typed input/output schemas on Operation (#3) — in progress; added to chat, chat-two-pass, search, recall, graph (see edits). **"Adding input schemas to the other 4 operations" was the last step called out.**
8. `support.invoke(kind, input)` composition primitive (#6) — shipped
9. `ctx.signal` AbortSignal threading (#5) — shipped
10. Streaming runner (#12) — shipped (`pipeline-stream.js`)
11. `AgentPipeline` parallel facade (#1) — scaffolded (`agent-pipeline.js`, `agent-operations/`)
12. Composite preset schema builder (#13) — **not started**

## Where it stopped
Last assistant line before limit: *"Running pipeline regression now."* The migration agent had just completed (7+3+3 new tests). The "ADR status flips + cross-refs" agent completed and is what tripped the notification.

## Uncommitted state

**trix-api** (new files):
- `src/lib/pipelines/memory-pipeline.js`, `pipeline-envelope.js`, `pipeline-runner.js`, `pipeline-stream.js`, `agent-pipeline.js`
- `src/lib/pipelines/operations/` (chat, chat-two-pass, search, recall, graph, shared, registry, index, register-all)
- `src/lib/pipelines/agent-operations/`
- `src/routes/memory-pipelines.js`
- `migrations/20260708000000_agent_preset_memory_pipeline.js`
- Tests: `tests/lib/pipelines/**`, `tests/migrations/agent-preset-memory-pipeline.test.js`, `tests/routes/memory-pipelines.test.js`, `tests/lib/chat/answer-format.test.js`

**trix-api** (modified): `chat-service.js`, `preset-resolver.js`, `preset-to-chat-options.js`, `pipeline-schema.js`, `AgentPresetRepository.js`, `agent-presets.js`, `chat.js`, `server.js`, plus test helpers and updated route tests.

**trix-research** (modified/new):
- ADR-108/109/110/111 moved planned→completed
- ADR-103 updated
- New: ADR-112 (memory-pipeline universal entrypoint), ADR-113 (pluggable operation registry)
- Note: legitimate trix-app frontend ADRs `ADR-112-webgl-lut-color-filters.md` and `ADR-113-ar-lenses-face-effects.md` had number conflicts with our memory-pipeline ADRs. Renamed to `ADR-114` and `ADR-115` respectively (Proposed status, uncommitted, no external refs).

## Immediate next steps when resuming
1. Run the pipeline regression (`cd trix-api && npm test -- tests/lib/pipelines` or the full suite) — this was in flight.
2. Finish input schemas on remaining operations (finding #7).
3. Composite preset schema builder (finding #13) — the remaining blocker before P7.
4. Then: ship quick-win commit, then architectural commit, then first benchmark run.

## Constraints
- Tight benchmark budget — don't rerun LoCoMo casually.
- File limit 300 lines (hard 500), function limit 25 (hard 40) — several new files are close, check before extending.
- Pre-launch: no backcompat needed, move fast.
